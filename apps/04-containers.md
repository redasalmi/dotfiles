# Containers and virtualization

Container, toolbox, guest, and virtualization tooling explicitly selected on the current Fedora system.

```text
qemu-guest-agent
toolbox
virtualbox-guest-additions
```
